# Buddy-specific dependencies (safe to pip install on Windows).
#
# Do NOT add speech-to-speech here — its default extras pull faster-qwen3-tts[ggml],
# which requires qwentts-cpp-python (no Windows wheels). Use setup-venv.ps1 instead:
#
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
#   .\setup-venv.ps1
#
# To switch TTS backends, set $ttsBackend in start-speech-to-speech.ps1.
# Pocket requires HF login + kyutai/pocket-tts terms accepted for voice cloning.

pocket-tts>=2.1.0
opencv-python-headless
pygrabber>=0.2; sys_platform == "win32"
mss
python-telegram-bot>=21.0
python-dotenv>=1.0.0
